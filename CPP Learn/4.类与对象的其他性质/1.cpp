/*
	建立一个类，该类具有const和非const成员函数.
	创建这个类的const和非const对象，并用不同类型的对象调用不同类型的成员函数。
	
*/
#include<iostream>
using namespace std;
class classa
{
	public:
		int a;
		int b;
		int sub(int a,int b);
		int add(int a,int b);
};
int main()
{
	
	return 0;
}
